# List of files relative to the server root (!) that will be marked for
# exclusion from indexing and other robotic spidering. * is supported. 
# Will only be effective if SITE_URL points to server root. The list is used
# to exclude resources from /robots.txt and /sitemap.xml, and to inform search
# engines about /sitemapindex.xml.
# ROBOTS_EXCLUSIONS = ["/archive.html", "/category/*.html"]
