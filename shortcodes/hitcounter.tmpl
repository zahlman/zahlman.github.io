<%
    slug = custom_slug or post.meta['en']['slug']
    base = "https://hits.sh/zahlman.github.io"
    old_text = " since January 1, 2025" if old else ''
    label = f"Hits{old_text} (powered by hits.sh)"
%>
## The default label colour is already #555, which is pretty close to
## 1/3 lightness in GIMP's colour picker. It should contrast nicely with
## the 2/3 lightness, max chroma bluish-green in the theme.
<a href="${base}+${slug}/"><img alt="${label}" src="${base}+${slug}.svg?view=today-total&label=${label|u}&color=02b983"/></a>
